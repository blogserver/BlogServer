spring:
  profiles:
    active:
      - dev
mybatis:
  mapper-locations: classpath*:com/opensource/soft/BlogServer/**/mapper/*Mapper.xml
  type-aliases-package: com.chinaopensource.soft.BlogServer
  
pagehelper:
  helper-dialect: mysql